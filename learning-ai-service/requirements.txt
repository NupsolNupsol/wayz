# Runtime dependencies for the LockerFlow learning/AI service.
# Pinned to minor versions: this service is deployed as an image, and a silent major bump in
# a transitive ML dependency is a two-hour debugging session nobody planned for.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# Server-to-server tokens signed by the LockerFlow Node API.
PyJWT==2.10.1

# Vector database client. 1.12 is the first release with query_points().
qdrant-client==1.12.2

# Document catalogue, beside the platform's existing MongoDB.
pymongo==4.10.1

# Local embeddings (BAAI/bge-m3). Torch is installed separately in the Dockerfile from the
# CPU index, which is ~200 MB rather than the ~2.5 GB CUDA build nothing here would use.
sentence-transformers==3.3.1

# Final answer generation.
#
# 3.x is required, not merely newer: `client.responses.create` — the API OpenAI currently
# recommends, and this service's default call style — does not exist before 1.66, and the SDK
# raises a bare AttributeError rather than anything diagnosable when it is missing.
openai==3.13.0

httpx==0.28.1
