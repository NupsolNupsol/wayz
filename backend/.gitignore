node_modules/
dist/
*.tsbuildinfo

# Environment: real values stay local, the template is committed.
.env
.env.*
!.env.example

*.log
test-results/
playwright-report/
.DS_Store

# ZATCA XML invoices persisted per tenant (binary artifacts, not source)
storage/
